<div dir="rtl" align="right">

# 🚀 الدليل الشامل لرفع وتشغيل المنصة (لغير المبرمجين)

مرحباً بك! تم إعداد هذا الدليل خصيصاً ليأخذ بيدك خطوة بخطوة وبطريقة مبسطة جداً، من استلام ملفات المشروع وحتى إطلاق موقعك ليكون متاحاً للجميع عبر الإنترنت، مع ضمان الحفاظ على سرية بياناتك بشكل كامل.

---

## 📂 المرحلة الأولى: رفع ملفات المشروع إلى GitHub
*(موقع GitHub هو الخزنة الآمنة التي ستحفظ كود مشروعك، وتعمل كجسر لنقله إلى السيرفر لاحقاً).*

1. اذهب إلى موقع [GitHub.com](https://github.com/) وقم بإنشاء حساب مجاني (Sign up).
2. بعد تسجيل الدخول، اضغط على الزر الأخضر **Create repository** (أو زر **New**).
3. في خانة **Repository name**، اكتب اسماً لمشروعك (مثال: `my-inventory-hub`).
4. ⚠️ **خطوة هامة جداً:** اختر **Private** ليكون الكود خاصاً بك ولا يراه أحد غيرك.
5. انزل لأسفل الصفحة واضغط على زر **Create repository**.
6. في الصفحة التالية، ستجد خياراً بالخط الأزرق يقول **uploading an existing file**، اضغط عليه.
7. قم بسحب وإفلات **جميع ملفات المشروع** (المجلد الذي استلمته من المطور) إلى هذه الصفحة.
8. انتظر قليلاً حتى يكتمل رفع جميع الملفات، ثم انزل للأسفل واضغط على الزر الأخضر **Commit changes**.

---

## 🔑 المرحلة الثانية: كيفية استخراج كل متغير بالتفصيل المُمِل
الموقع يحتاج لعدة مفاتيح لكي يعمل. سنقوم هنا بشرح كيفية استخراج كل مفتاح بالتفصيل خطوة بخطوة من موقعه الخاص.

### 1️⃣ مفاتيح قاعدة البيانات (من موقع Supabase)
*(Supabase هو النظام الذي سيحفظ جداول الموقع وبيانات المستخدمين).*

1. اذهب إلى [Supabase.com](https://supabase.com/)، أنشئ حساباً مجانياً واضغط على **Start your project**.
2. اضغط على **New Project**، اختر اسماً للمشروع، وضع **كلمة مرور قوية جداً** لقاعدة البيانات *(اكتبها في ورقة أو ملف نصي لأنك ستحتاجها بعد قليل)*.
3. انتظر بضع دقائق حتى ينتهي بناء المشروع.
4. **لاستخراج المتغير `VITE_SUPABASE_URL` والمتغير `VITE_SUPABASE_ANON_KEY` المتغير `SUPABASE_SERVICE_ROLE_KEY`:**
   - انظر إلى القائمة الجانبية اليسرى، وابحث عن أيقونة الترس ⚙️ (Project Settings) واضغط عليها.
   - من القائمة المنسدلة للترس، اختر **API**.
   - ستجد في الصفحة قسم اسمه **Project URL** تحته رابط. انسخ هذا الرابط، هذا هو الـ `VITE_SUPABASE_URL`.
   - انزل قليلاً في نفس الصفحة لقسم **Project API keys**. ستجد مفتاحين:
     - الأول مكتوب بجانبه (anon / public). انسخه، هذا هو الـ `VITE_SUPABASE_ANON_KEY`.
     - الثاني مكتوب بجانبه (service_role / secret). اضغط على زر "Reveal" لإظهاره ثم انسخه، هذا هو الـ `SUPABASE_SERVICE_ROLE_KEY`.
5. **لاستخراج المتغير `DATABASE_URL`:**
   - من نفس القائمة الجانبية تحت أيقونة الترس ⚙️، اختر **Database** (أو Configuration > Database).
   - انزل لأسفل الصفحة حتى تجد قسماً يسمى **Connection string**.
   - اختر التبويب المكتوب عليه **URI**.
   - ستجد رابطاً يبدأ بـ `postgresql://...`، قم بنسخه.
   - ⚠️ **ملاحظة هامة:** هذا الرابط سيحتوي في منتصفه على كلمة `[YOUR-PASSWORD]`. قم بمسح هذه الكلمة مع الأقواس المربعة، واكتب مكانها "كلمة المرور القوية" التي أنشأتها في الخطوة رقم 2. بعد التعديل، يصبح هذا الرابط هو الـ `DATABASE_URL`.
6. **بناء جداول البيانات (خطوة ضرورية):**
   - في القائمة الجانبية اليسرى لـ Supabase، ابحث عن أيقونة **SQL Editor** (أيقونة نافذة أو كود) واضغط عليها.
   - اضغط على **New query**.
   - افتح ملف `supabase-init.sql` (الموجود ضمن ملفات المشروع التي أرسلها لك المطور) باستخدام مفكرة (Notepad)، وانسخ كل محتواه.
   - الصق المحتوى في المحرر في Supabase واضغط على زر **Run** الأخضر أسفل أو أعلى الشاشة. (هذا سيبني جداول الموقع بضغطة زر).

### 2️⃣ مفاتيح تسجيل الدخول (من موقع Microsoft Azure)
*(ضروري للسماح لك وللموظفين بتسجيل الدخول وجلب الملفات من حساب OneDrive).*

1. اذهب إلى [بوابة Microsoft Azure](https://portal.azure.com/) وسجل الدخول بحساب مايكروسوفت الخاص بك.
2. ابحث في شريط البحث العلوي (في منتصف الشاشة من الأعلى) عن كلمة **App registrations** واخترها.
3. اضغط على الزر بالأسهم ➕ **New registration**.
4. اكتب اسماً للتطبيق (مثال: My Inventory App).
5. تحت خيار *Supported account types*، اختر الدائرة المكتوب بجانبها **(Accounts in any organizational directory and personal Microsoft accounts)** إذا أردت السماح لأي شخص بالدخول بحساب مايكروسوفت الشخصي أو المهني.
6. في قسم *Redirect URI* (في الأسفل):
   - اختر **Web** من القائمة المنسدلة.
   - اكتب هذا الرابط المؤقت في المربع: `https://test.vercel.app/api/auth/microsoft/callback` *(سنشرح لك كيف تعدّله لاحقاً)*.
7. اضغط على زر **Register** الأزرق في الأسفل.
8. ستنتقل الآن إلى صفحة التطبيق الأساسية (Overview).
   - **لاستخراج المتغير `VITE_MICROSOFT_CLIENT_ID`:**
     - ستجد في أعلى الصفحة قيمة باسم **Application (client) ID**. انسخ الرقم المجاور لها، هذا هو مفتاح الـ Client ID.
   - **لاستخراج المتغير `MICROSOFT_TENANT_ID`:**
     - في نفس الصفحة، ابحث عن **Directory (tenant) ID**. انسخ الرقم المجاور لها (أو ببساطة يمكنك استخدام الكلمة الثابتة `common` في Vercel لاحقاً لتسهيل الأمر).
9. **لاستخراج المتغير `MICROSOFT_CLIENT_SECRET`:**
   - من القائمة الجانبية اليسرى في صفحة التطبيق، اضغط على **Certificates & secrets**.
   - في منتصف الصفحة، تأكد أنك في تبويب Client secrets واضغط على ➕ **New client secret**.
   - اكتب أي وصف في المربع (مثلاً: Secret 1) واضغط Add.
   - ⚠️ **هام جداً:** سيظهر لك جدول جديد. اذهب إلى عمود **Value** وانسخ النص الطويل الموجود فيه فوراً ولن يظهر لك مرة أخرى أبداً! هذا هو الـ Secret الخاص بك.

### 3️⃣ المتغيرات الخاصة بك (أنت تكتبها من رأسك)
هذه المتغيرات لا تحتاج لاستخراجها من أي موقع، بل أنت من تبتكرها:
1. **المتغير `VITE_APP_ID`:**
   - فقط اكتب اسماً باللغة الإنجليزية لتطبيقك (مثال: `my-super-app`).
2. **المتغير `JWT_SECRET`:**
   - هذا بمثابة قفل تشفير الجلسات. اضرب على الكيبورد عشوائياً لإنتاج نص طويل جداً بالإنجليزية وبلا معنى (مثال: `alksjdflkjasdhf897234lkjhasdf987234`). انسخه واحتفظ به.
3. **المتغير `MICROSOFT_REDIRECT_URI`:**
   - سنجهزه الآن. سيكون عبارة عن رابط موقعك المستقبلي في Vercel مضافاً إليه مسار الدخول. سيكون شكله النهائي هكذا: `https://[اسم-مشروعك].vercel.app/api/auth/microsoft/callback`.

### 4️⃣ متغيرات من المطور أو منصات خارجية (الذكاء الاصطناعي والإحصائيات)
هذه المتغيرات مخصصة لربط أنظمة الذكاء الاصطناعي والإحصائيات التابعة لجهة خارجية:
1. **المتغيرات `BUILT_IN_FORGE_API_URL` و `BUILT_IN_FORGE_API_KEY` و `OAUTH_SERVER_URL`:**
   - هذه مفاتيح تخص سيرفر الذكاء الاصطناعي والمصادقة المركزي.
   - **كيف أحصل عليها؟** اطلب هذه المفاتيح والروابط من **المطور** الذي سلّمك المشروع، أو إذا كنت تملك حساباً في منصة ذكاء اصطناعي (مثل OpenRouter) يمكنك وضع مفتاح الـ API الخاص بك هنا.
2. **المتغيرات `VITE_ANALYTICS_ENDPOINT` و `VITE_ANALYTICS_WEBSITE_ID`:**
   - تُستخدم لتتبع زوار الموقع (عبر منصات مثل Umami).
   - **كيف أحصل عليها؟** إذا لم تكن تهتم بالإحصائيات أو لم تقم بالتسجيل في منصة إحصائيات خارجية، ببساطة اترك قيمتها **فارغة تماماً** (لا تكتب شيئاً).

---

## 🌐 المرحلة الثالثة: إطلاق الموقع على Vercel وإضافة المتغيرات
*(Vercel هو السيرفر المجاني الذي سيشغل موقعك ويجعله متاحاً على الإنترنت، وفيه سنجمع كل المفاتيح التي استخرجناها).*

1. اذهب إلى [Vercel.com](https://vercel.com/) وأنشئ حساباً باختيار **Continue with GitHub**.
2. اضغط على الزر الأسود **Add New...** في أعلى اليمين ثم اختر **Project**.
3. ستجد مستودعك الذي رفعته في المرحلة الأولى، اضغط بجانبه على **Import**.
4. في صفحة الإعدادات، انزل للأسفل إلى قسم **Environment Variables**. افتحه وقم بإضافة المتغيرات التالية التي جمعناها (تضع الاسم في Name والقيمة في Value ثم تضغط Add لكل واحد):

| اسم المتغير (Name) يجب نسخه بدقة | القيمة المطلوبة (Value) |
| :--- | :--- |
| `VITE_APP_ID` | اكتب اسماً لمرة واحدة من رأسك (مثل: `my-platform`). |
| `VITE_SUPABASE_URL` | الرابط الذي نسخته من إعدادات API في Supabase. |
| `VITE_SUPABASE_ANON_KEY` | مفتاح anon الطويل الذي نسخته من Supabase. |
| `VITE_MICROSOFT_CLIENT_ID` | مفتاح Application ID الذي نسخته من منصة Azure. |
| `DATABASE_URL` | رابط قاعدة البيانات من Supabase **بعد** أن وضعت كلمة المرور بداخله. |
| `SUPABASE_SERVICE_ROLE_KEY` | مفتاح service_role السري من Supabase. |
| `MICROSOFT_CLIENT_SECRET` | نص Value السري الذي نسخته من Azure. |
| `MICROSOFT_TENANT_ID` | اكتب كلمة `common` بالإنجليزية، أو رقم Tenant الخاص بك. |
| `JWT_SECRET` | نص عشوائي طويل جداً من رأسك لتشفير الجلسات. |
| `BUILT_IN_FORGE_API_URL` | رابط خادم الذكاء الاصطناعي (أحضره من المطور). |
| `BUILT_IN_FORGE_API_KEY` | مفتاح الذكاء الاصطناعي السري (أحضره من المطور). |
| `OAUTH_SERVER_URL` | رابط سيرفر المصادقة (أحضره من المطور). |
| `VITE_ANALYTICS_ENDPOINT` | *(اختياري)* رابط الإحصائيات، أو اتركه فارغاً. |
| `VITE_ANALYTICS_WEBSITE_ID` | *(اختياري)* مُعرف الموقع، أو اتركه فارغاً. |

5. بعد أن تضغط Add لكل هذه المتغيرات، اضغط على الزر الكبير **Deploy**.
6. انتظر حوالي دقيقتين أو ثلاث. ستظهر شاشة احتفالية بنجاح العملية! اضغط على زر **Continue to Dashboard**.
7. في هذه الصفحة الجديدة، ستجد رابط موقعك الحقيقي مكتوباً بالخط العريض (يبدو هكذا: `https://my-project-abcd.vercel.app`). انسخه.

---

## ✅ المرحلة الرابعة والأخيرة: تحديث رابط مايكروسوفت النهائي
الآن بعد أن أصبح لموقعك رابط حقيقي في Vercel، علينا إخبار مايكروسوفت به لكي يعمل زر "تسجيل الدخول":

1. اذهب إلى منصة Azure مجدداً > App registrations > التطبيق الذي أنشأته.
2. اذهب إلى **Authentication** من القائمة الجانبية اليسرى.
3. في قسم Redirect URIs، امسح الرابط المؤقت القديم، وضع رابط موقعك الحقيقي الجديد مع إضافة `/api/auth/microsoft/callback` في نهايته.
   *(ليصبح هكذا بالضبط: `https://[رابط-موقعك-الحقيقي]/api/auth/microsoft/callback`)*.
4. انزل للأسفل واضغط على زر الحفظ (Save).
5. ارجع إلى مشروعك في Vercel، واذهب إلى تبويب **Settings** بالأعلى، ثم من القائمة اختر **Environment Variables**.
6. أضف هذا المتغير الأخير:
   - Name: `MICROSOFT_REDIRECT_URI`
   - Value: نفس الرابط الطويل الذي وضعته في الخطوة 3 للتو.
   - ثم اضغط Add.
7. لتفعيل هذا التغيير الأخير ليأخذ مجراه: من القائمة العلوية في Vercel اختر **Deployments**، اضغط على النقاط الثلاث العمودية بجوار أحدث عملية نشر في القائمة، واختر **Redeploy**.

🎉 **ألف مبروك!** موقعك الآن جاهز بالكامل ويعمل بأمان تام ولا يرتبط بأي جهة خارجية.

</div>
